const layout = @import("layout.zig");
const view_models = @import("../view_models.zig");

pub templ ErrorPage(view: view_models.ErrorPage) {
    @layout.Layout(view.title) {
        <div class="panel">
            <h1>{view.title}</h1>
            <p>{view.message}</p>
            <a href="/">Back to dashboard</a>
        </div>
    }
}
